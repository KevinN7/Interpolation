function P = lagrangeSurfAna(ptsCtrl,t1,t2,u,v)

[n,m,~] = size(ptsCtrl);

lagrange()

res = 0;
for i=1:n
    for j=1:m
        res = res + ptsCtrl(i,j)*lagrange()*lagrange(); 
    end;
end; 

val = res;